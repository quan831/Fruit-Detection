# Pin only the essentials; if you have GPU, install torch per your CUDA:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
ultralytics==8.3.34
opencv-python==4.10.0.84
gradio>=4.0.0
brotli>=1.1.0
# torch  # (install separately to match your environment/CUDA)